{
  "name": "@1week/themes",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build.js",
    "build.js": "node build js"
  },
  "devDependencies": {
    "esbuild": "^0.19.11"
  }
}
